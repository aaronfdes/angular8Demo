import { CustomerColumnFilterPipe } from './customer-column-filter.pipe';

describe('CustomerColumnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerColumnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
